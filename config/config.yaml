app:
  name: RustFast
  host: 0.0.0.0
  port: 8080
  debug: true

log:
  level: info

database:
  driver: sqlite
  path: database.db

cache:
  driver: memory

jwt:
  secret: "your-secret-key-change-in-production"
  ttl: 86400
